{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo \"Vite removed: build no-op\"",
        "start": "echo \"Vite removed: use Laravel/PHP server or configure alternative\""
    },
    "devDependencies": {
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "tailwindcss": "^4.0.0"
    }
}
